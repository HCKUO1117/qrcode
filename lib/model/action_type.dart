enum ActionType {
  launchApp,
  copy,
  search,
  launchUrl,
  saveBookmark,
  sendEmail,
  sendSms,
  call,
  openMap,
  connectWifi,
  saveContact,
  saveCalendar
}

extension ActionTypeEx on ActionType{
}
